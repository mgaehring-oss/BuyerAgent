# Local Claude settings (permissions, local overrides)
.claude/settings.local.json

# Google OAuth credentials (never commit real credentials)
resources/google-credentials.json

# Keep output folder tracked but ignore generated reports
# (remove the line below if you want reports committed to git)
output/*.md
